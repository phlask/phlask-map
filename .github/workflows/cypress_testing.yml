name: Cypress Tests

on: [workflow_call]

jobs:
  desktop:
    uses: cypress-io/github-action@v6
    with:
      build: yarn build
      start: REACT_APP_CYPRESS_TEST=true yarn start
      browser: chrome
